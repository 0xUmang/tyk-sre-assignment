apiVersion: apps/v1
kind: Deployment
metadata:
  name: tyk-sre-python
  labels:
    app: tyk-sre-python
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: tyk-sre-python
  template:
    metadata:
      labels:
        app: tyk-sre-python
    spec:
      serviceAccountName: tyk-sre-python
      containers:
        - name: tyk-sre-python
          image: "{{ .Values.image }}"
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3

