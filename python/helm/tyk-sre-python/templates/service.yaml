apiVersion: v1
kind: Service
metadata:
  name: tyk-sre-python
  labels:
    app: tyk-sre-python
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: tyk-sre-python
